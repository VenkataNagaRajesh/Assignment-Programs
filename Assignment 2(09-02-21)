#include<stdio.h>
int main()
{
   int a[50];
   int i,s=0,n;
   int *p;
    printf("enter the range of the array");
    scanf("%d%,&n);
   printf("\nEnter array elements (%d integer values):",n);
   for(i=0;i<n;i++)
      scanf("%d",&array[i]);
   ptr = array;
   for(i=0;i<n;i++) 
   {
      s = s + *p;
      p++;
   }
   printf("\nThe sum is: %d",s);
}
